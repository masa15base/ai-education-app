# Vercel Production Environment Variables (Project Settings → Environment Variables)
# Copy keys into the Vercel dashboard — do not commit real secrets here.

# Heroku backend API base (must end with /api)
VITE_API_URL=https://ai-edu-app-backend-fb6ffb49064a.herokuapp.com/api

# Production: leave unset or false (anonymous image upload is dev/E2E only)
# VITE_ALLOW_ANONYMOUS_MEDIA=
